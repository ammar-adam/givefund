PORT=8000
DB_PATH=./givefund.db

# Frontend origin for Stripe Checkout redirects (no trailing slash)
GIVEFUND_FRONTEND_URL=http://127.0.0.1:5500

# Stripe — enable optional Link enrollment via /checkout/link-setup
# Dashboard: enable Link under Settings → Payment methods
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=

# GlobalGiving API (free registration) — enables globalgiving scraper
# GLOBALGIVING_API_KEY=

# Live ingestion (production: LIVE_SCRAPE=true in start_prod.sh)
LIVE_SCRAPE=true
SCRAPE_INTERVAL=1800
SCRAPE_PARALLEL=4
SCRAPE_ON_START=true
MIN_CAMPAIGNS=500

# GoFundMe Algolia scale (set in GitHub secrets for Actions)
# GFM_ALGOLIA_APP_ID=
# GFM_ALGOLIA_API_KEY=
# GFM_ALGOLIA_ONLY=true
# GFM_MAX_ALGOLIA_PAGES=60
# LAUNCHGOOD_MAX_ENRICH=120

# GoFundMe scraper: set after one successful browser capture (DevTools → Algolia request headers)
# GFM_ALGOLIA_APP_ID=
# GFM_ALGOLIA_API_KEY=
# GFM_ALGOLIA_ONLY=true
